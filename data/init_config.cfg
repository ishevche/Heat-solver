top="100.0"     # may depend on x, changes from left to right
right="100.0"   # may depend on x, changes from top to bottom
bottom="100.0"  # may depend on x, changes from left to right
left="100.0"    # may depend on x, changes from top to bottom
plate="0.0"     # may depend on x, and y